  <div class="contents row">
    <%= form_with url: "/tweets/#{@tweet.id}", method: :patch, local: true do |form| %>
      <h3>
        編集する
      </h3>
      <textarea cols="30" name="text" placeholder="text" rows="10"><%= @tweet.text %></textarea>
      <%= form.submit %>
    <% end %>
  </div>
